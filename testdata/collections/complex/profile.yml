info:
  name: Profile
  type: http
  seq: 2
http:
  method: GET
  url: https://api.example.com/profile
chain:
  - alias: login
    request: Auth/Login
scripts:
  preRequest: |
    request.headers["X-Trace"] = String(Date.now());
