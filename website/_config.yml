# Jekyll configuration for the Helena project website (#64).
# Self-contained: custom layouts + CSS, no gem theme, no plugins - so it builds
# on GitHub Pages' stock Jekyll. Lives in website/ so it can be deployed later
# (e.g. a GitHub Action that builds this folder, or by moving it to the Pages
# source). See website/README.md for deployment.

title: Helena
tagline: A native, lightweight API client
description: >-
  Helena is a fast, native API client for Linux, macOS, and Windows - a
  lightweight take on the usual desktop API clients, built with Go and Fyne. One
  small binary, plain-text collections, nine auth schemes, scripting, SSE, and
  WebSocket.
author: Bartosz Pachołek
# Copyright line + author/company link rendered in the footer and About page.
copyright: "IDCT · Bartosz Pachołek"
author_url: "https://idct.tech"

# Deployed as a GitHub Pages PROJECT page under the ideaconnect org's apex custom
# domain, so the site lives at https://idct.tech/helena/ — url is the apex and
# baseurl is the project path. The apex domain is owned by the org's
# ideaconnect.github.io page repo; this project repo carries NO CNAME (a project
# CNAME would try to claim the domain at its root and conflict). Internal links
# use relative_url/absolute_url, so baseurl must stay in sync with the deploy.
url: "https://idct.tech"
baseurl: "/helena"

repo: "https://github.com/ideaconnect/helena"
releases: "https://github.com/ideaconnect/helena/releases"
discord: "https://discord.gg/uYsuaa8HNm"
sponsor: "https://github.com/sponsors/ideaconnect"
coffee: "https://buymeacoffee.com/idct"

# Pretty URLs: each page builds to <name>/index.html so the nav's "/features/",
# "/contact/", … links resolve (GitHub Pages doesn't map "/x/" to "x.html").
permalink: pretty

# Built with custom layouts under _layouts/; no remote theme.
exclude:
  - Gemfile
  - Gemfile.lock
  - README.md
  - vendor

# Nav order is defined here and rendered by _layouts/default.html.
nav:
  - { title: "Features", url: "/features/" }
  - { title: "Roadmap", url: "/roadmap/" }
  - { title: "Examples", url: "/examples/" }
  - { title: "About", url: "/about/" }
  - { title: "Contact", url: "/contact/" }
